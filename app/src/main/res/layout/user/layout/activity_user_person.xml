<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
xmlns:android="http://schemas.android.com/apk/res/android"
xmlns:app="http://schemas.android.com/apk/res-auto"
xmlns:tools="http://schemas.android.com/tools"
android:layout_width="match_parent"
    android:background="@color/abuabumuda"
android:layout_height="match_parent"
tools:context=".user.UserPerson">

<TextView
    android:id="@+id/tv_nama_user_person"
    android:layout_marginVertical="50sp"
    android:textSize="35sp"
    android:textColor="@color/hitam"
    android:text="Madee"
    android:layout_centerHorizontal="true"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"/>
    <TextView
        android:layout_below="@id/tv_nama_user_person"
        android:id="@+id/tv_email_user_person"
        android:textSize="25sp"
        android:textColor="@color/hitam"
        android:text="Madee@gmail.com"
        android:layout_centerHorizontal="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>
    <Button
        android:id="@+id/btn_logout_user"
        android:layout_marginVertical="20dp"
        android:layout_below="@id/tv_email_user_person"
        android:layout_centerHorizontal="true"
        android:background="@drawable/btn_18_blue"
        android:textColor="@color/white"
        android:textAllCaps="false"
        android:text="Logout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

<com.google.android.material.bottomnavigation.BottomNavigationView
    android:id="@+id/bottom_navigation"
    android:layout_width="match_parent"
    android:layout_height="60dp"
    android:layout_alignParentBottom="true"
    app:itemBackground="@color/biru"
    app:itemIconTint="@drawable/selector"
    app:itemTextColor="@drawable/selector"
    app:menu="@menu/menu_navigation" />

</RelativeLayout>